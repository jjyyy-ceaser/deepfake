🧪 실험 데이터셋 및 모델 평가 설계 (최종 확정본 - Rev.18)1. 데이터 소스 (Source Data)가짜 영상 (Fake): Stable Video Diffusion (SVD)생성 방식: Stability AI의 최신 비디오 확산 모델인 SVD (img2vid-xt) 프레임워크 기반. 본 연구에서는 1_generate_svd_final.py를 통해 전처리된 실제 영상의 첫 번째 프레임을 조건부 입력(Conditional Input)으로 사용하여 생성함.특징: 잠재 공간 확산(Latent Diffusion)을 통해 참조 이미지의 컨텐츠를 유지하면서 시간적 일관성을 가진 비디오를 생성. 기존 GAN 방식보다 정교한 시공간적 특징을 가지나, 디퓨전 특유의 미세 노이즈 패턴이 존재함.근거: **Blattmann et al. (2023)**의 연구에 따르면, SVD는 고해상도 비디오 생성에서 SOTA 성능을 보이며, 이는 현재 가장 탐지하기 어려운 최신 생성 모델 도메인을 대표함.SVD 생성 시나리오 구체화: 변인 통제를 위해 SVD 생성 파라미터를 motion_bucket_id=127, noise_aug_strength=0.02, fps=25, seed=42로 고정함.이유: 생성 옵션의 차이가 아닌 조작 흔적 자체를 학습하기 위한 필수 설정임.근거: 해당 수치는 Stability AI에서 권장하는 표준값으로, 육안상 실제 영상과 구분이 어려운 중상급 품질(High-fidelity)의 동적 비디오를 생성하므로 탐지 모델의 변별력을 테스트하기에 가장 적합한 임계점임.실제 영상 (Real): CelebV-HQ (Original Sequences)수집 방식: 고화질 인물 비디오 데이터셋인 CelebV-HQ에서 300개의 원본 클립을 선별.특징: 실제 인물의 고해상도 안면 특징, 다양한 조명 조건, 자연스러운 미세 표정 변화를 포함함.근거: **Zhu et al. (2022)**에서 제안된 CelebV-HQ는 딥페이크 탐지 연구에서 실제 인간의 고품질 데이터 기준점(Baseline)으로 널리 사용됨.(기존 2. 플랫폼별 변조 매트릭스 섹션은 데이터 재추출을 위해 제외됨)2. 데이터셋 구성 및 분할 (Dataset Split)언페어드 분할 (Unpaired Balanced Split): CelebV-HQ(Real) 300개와 SVD(Fake) 300개를 결합하여 총 600개 클립으로 구성. 1:1 클래스 균형을 유지함.데이터 분할: 전체의 **80%(학습), 20%(테스트)**로 분리하여 인물의 외모가 아닌 조작 특징을 학습하도록 설계함. 특히, 데이터 분할 시 클립 단위를 넘어 **인물 식별자(Identity) 기반 분리(Identity-level Split)**를 수행함.이유: 데이터 누수(Data Leakage)를 원천 차단하고 모델의 일반화 능력을 객관적으로 평가함.교차 검증: 5-Fold Cross-Validation을 통해 데이터셋의 통계적 안정성을 확보함.3. 탐지 모델 라인업 (Model Lineup)세대별(Classic vs. SOTA) 및 구조별(Spatial vs. Temporal vs. Hybrid) 대표 모델 5종을 선정했습니다.A. Spatial (공간적): ① Xception (Classic), ② Swin Transformer-Tiny (New)B. Temporal (시간적): ③ R3D-18 (Classic), ④ VideoMAE V1 (New)C. Hybrid (하이브리드): ⑤ ConvNeXt-Tiny + GRU (New Hybrid)4. 학습 및 평가 전략 (Cross-Domain Evaluation)학습: Raw(원본) 데이터로만 학습하여 생성 모델의 순수 특징(Fingerprint) 추출에 집중함.성능 지표: 탐지 모델의 포괄적 성능 평가를 위해 ①ACC, ②AUC, ③F1-Score, ④Precision, ⑤Recall 및 포렌식 신뢰성 검증을 위한 **⑥EER (Equal Error Rate)**과 **BPCER/APCER (Presentation Classification Error Rate)**를 산출하여 총 6가지 이상의 다각적 지표를 분석함.근거: ISO/IEC 19795-1 표준에 따라 위조를 진짜로 판정한 오류(APCER)와 진짜를 위조로 판정한 오류(BPCER)를 분리 보고함으로써 포렌식 신뢰도를 입증함.5. 전처리 및 입력 규격 (Data Preprocessing)입력 규격: Spatial/Hybrid/VideoMAE ($224 \times 224$), R3D ($112 \times 112$).표준화 파이프라인 (0_standardize.py):프레임 레이트 및 길이 동기화 (Temporal Synchronization): 모든 Real 및 Fake 영상을 **25 FPS / 25 프레임(1.0초)**으로 통일함.이유: 데이터셋 간의 미세한 시간적 해상도 차이가 모델의 학습 편향(Bias)으로 작용하는 것을 방지하고, Real의 높은 FPS와 Fake의 낮은 FPS 차이를 모델이 조작 흔적으로 오인하는 'Temporal Shortcut' 문제를 원천 차단하기 위함임.동적 비율 크롭 (Dynamic Scaling Crop): MTCNN을 통해 첫 프레임의 안면 위치를 탐지한 후, 얼굴 높이 대비 2.5배(scale=2.5) 영역을 16:9 비율로 계산하여 크롭함.이유: 머리카락 상단부터 턱 아래 목선까지의 핵심 정보를 안정적으로 포함하여, 딥페이크 탐지의 주요 지표인 안면 주변부 왜곡(Peripheral Artifacts)을 보존하기 위함임. (논문 기술 시에는 **"랜드마크 중심의 시공간적 변동성을 최소화한 Smart Fit 정렬"**이라고 표기함)위치 이동 로직 (Shift-based Boundary Handling): 크롭 영역이 영상 경계를 벗어날 경우 검은색 패딩 대신 **크롭 윈도우를 영상 안쪽으로 이동(Shift)**시킴.보간법: 리사이즈 시 고주파 성분 보존을 위해 Lanczos4 Interpolation을 적용함.안면 정렬 (Face Alignment): 매 프레임 MTCNN을 활용해 바운딩 박스 중심 기반의 Smart Fit Alignment를 수행하여 안면 위치를 정규화함.근거: **Rossler et al. (2019)**에 따라, 안면 위치 고정 전처리를 통해 모델이 움직임 노이즈가 아닌 '안면 내부의 조작 불일치'에만 집중하도록 설계함.시간적 특징 추출 (Temporal Sampling): SVD의 25프레임 생성 결과물 중 촘촘한 샘플링(Dense Sampling)을 위해 Sliding Window 기반 Overlapping 샘플링을 적용함. (예: 1~16, 5~20, 9~25 프레임 구간 활용)6. 하드웨어 최적화 및 증강 전략 (Hardware & Augmentation)장비 사양: NVIDIA RTX 4070 Super (VRAM 12GB) + AMD Ryzen 5 7500F데이터 증강 전략 (Data Augmentation): 소규모 데이터셋($N=600$)의 한계를 극복하기 위해 Mixup ($\alpha=0.2$) 및 CutMix를 적용함.근거: **Zhang et al. (2017)**과 **Yun et al. (2019)**의 연구에 따라, 클래스 간 경계를 완화하고 특정 인물의 픽셀 암기를 방지하여 일반화 성능을 비약적으로 향상시킴.최적화: Mixed Precision (FP16) 및 VideoMAE용 Gradient Accumulation (4 Steps) 필수 적용.7. 연구 분석 방향성 (Analysis Focus)7-1. SNS 플랫폼 왜곡 요인 정량 분석 및 코드 정합성 (Mapping)설계서 요구사항 (Rev.18)코드 구현 현황 (6_analyze_distortion_specs.py)이유 및 학술적 근거 (Basis)[1절] SVD 특성 (Video Only)v:0 (비디오 스트림) 전용 추출. 오디오 배제.[데이터 순수성] 시각적 아티팩트 분석 효율 극대화. (Blattmann et al., 2023)[2-1절] 코덱/해상도/압축률width, height, codec_name, bit_rate[정보 밀도 파괴] 비트레이트 하락에 의한 공간 왜곡 측정. (Montibeller et al.)[8절] 플랫폼 왜곡 행렬 평가pix_fmt(크로마), color_space(색공간)[색상 채널 불일치] 색상 정보 손실에 따른 탐지 성능 분석. (Zhu et al., 2022)[9절] 시간적 모델 파괴 원인r_frame_rate(FPS), Total_Frames[시간적 연속성 파괴] 프레임 유실이 어텐션 맵에 미치는 영향 분석. (Feichtenhofer et al., 2019)[10.2절] 포렌식 근거 역추적analyze_mp4_atoms (moov, mdat)[데이터 재조합 증거] 플랫폼의 강제 재조립(Re-muxing) 증명. (Yang et al., 2024)⚠️ [학술적 메모] 데이터 분석 및 평가 상의 우려 사항우려 사항 1: Kakao Normal (24fps)의 강제 패딩(Padding) 부작용원인: distortion_analysis_report.csv에 따르면 카카오톡 노멀 화질은 1프레임이 유실되어 24프레임으로 수집됨.결과: dataset.py의 강제 패딩 로직에 의해 마지막 프레임에서 화면이 일시 정지하거나 덜컥거리는 현상이 발생하며, 이 인위적인 시간축 교란이 VideoMAE의 어텐션(Attention)을 교란하여 AUC 폭락을 야기함.우려 사항 2: 평가 임계값(Threshold) 설정의 통계적 한계 (Oracle Performance)현상: 실제 서비스라면 영상이 들어오는 족족 판정해야 하지만, 본 실험 코드는 **"전체 시험 결과를 다 본 뒤에 최적의 기준선을 긋는 방식(EER 최적화 등)"**을 따릅니다.착시 효과: 예를 들어 유튜브 도메인에서 모델 점수가 0.2~0.4로 낮게 분포할 때, 고정 임계값(0.5)은 전부 '진짜'로 오판하지만, 동적 임계값은 기준선을 0.3으로 낮춰 정확도를 보정합니다.학술적 의미: 이는 모델의 순수한 실력보다 데이터의 분포에 맞춰 기준선을 옮겨준 덕분에 성능이 더 좋아 보이는 현상을 일으키며, 이를 학술적으로 **'오라클 성능(Oracle Performance)'**이라 부릅니다. 논문 작성 시 이를 '이상적 상황에서의 탐지 상한선'임을 명시하여 객관성을 확보해야 합니다.8. 모델별 통합 정밀 사양 및 하이퍼파라미터 최적화 계획 (Architectural Specification)8-1. 실험의 형평성을 위한 데이터 샘플 수 통일 전략전략: Temporal/Hybrid 모델이 중첩 샘플링을 통해 클립당 3개의 시퀀스(총 1,800개)를 생성할 경우, Spatial 모델 역시 클립당 3장의 프레임을 독립적으로 추출하여 총 1,800개로 일치시킴. (구현 시 dataset.py의 samples_per_clip = 3 설정을 통해 Total logical samples: 1800을 보장함)근거: **Rezatofighi et al. (2019)**의 연구에 따르면, 학습 샘플당 역전파(Backpropagation) 횟수를 동일하게 맞추는 것이 구조적 우위를 증명하는 가장 객관적인 방법임.8-2. 모델별 세부 사양 및 가중치 운용 전략1. Xception (Spatial - Baseline)[고정 사양] 아키텍처: $224 \times 224 \times 3$ / MTCNN 안면 정렬 / Frozen (First 2 Blocks).이유: 하위 레이어는 범용적 특징을 추출하므로 동결하여 사전 학습된 지식을 보존하고, 상위 레이어에서 SVD 특유의 질감 아티팩트를 학습하도록 유도함. (근거: Rossler et al. 2019, Yosinski et al. 2014)[그리드 탐색 구간 및 근거] Learning Rate ($1e^{-3} \sim 1e^{-5}$), Dropout Rate ($0.2 \sim 0.6$).이유: 전이 학습 시 가중치의 수렴 속도와 과적합 임계점 사이의 최적 균형을 찾기 위한 표준 범위임. (근거: Srivastava et al., 2014)[최적 설정 및 분석] lr=1e-3, dropout=0.4, bs=32 (AUC 0.9987 | EER 0.0000)High LR 선호: FF++ 가중치가 이미 수렴점에 가까워, 높은 학습률($1e^{-3}$)이 지역 최적점 탈출에 유리했음.Dropout의 중용: 0.4에서 최고 성능을 기록하며 정규화의 균형을 확보함.2. Swin Transformer-Tiny (Spatial - SOTA)[고정 사양] 윈도우 $7 \times 7$ / 패치 $4 \times 4$.이유: 사전 학습된 Relative Position Bias와의 호환성을 위해 고정하며, $4 \times 4$ 패치는 디퓨전 모델의 미세 텍스처 불일치를 포착하는 최소 단위임. (근거: Liu et al. 2021)[그리드 탐색 구간 및 근거] Learning Rate ($1e^{-4} \sim 1e^{-5}$), Weight Decay ($0.01 \sim 0.1$).이유: 트랜스포머의 학습 불안정성을 제어하고 가중치 감쇄를 통한 정교한 규제 효과를 확인하기 위함임. (근거: Touvron et al., 2021)[최적 설정 및 분석] lr=1e-4, weight_decay=0.1, bs=16 (AUC 1.0000 | EER 0.0000)완벽한 분리: 계층적 윈도우 어텐션이 미세 픽셀 부조화를 완벽히 포착함.규제의 승리: 가장 높은 Weight Decay(0.1)에서도 성능을 유지하여 모델의 강력한 특징 추출 능력을 입증함.3. R3D-18 (Temporal - Baseline)[고정 사양] 해상도 $112 \times 112$ / $(3 \times 3 \times 3)$ 커널.이유: 3D 연산량 폭증을 제어하고 프레임 간 시공간적 일관성을 효율적으로 분석함. (근거: Tran et al. 2018)[그리드 탐색 구간 및 근거] Learning Rate ($1e^{-3} \sim 1e^{-4}$), Window Size (12 vs 16).이유: SVD 영상의 시공간적 결함(Artifact) 밀도가 집중되는 최적의 시간적 해상도를 검증하기 위함임. (근거: Feichtenhofer et al., 2019)[최적 설정 및 분석] lr=1e-4, window_size=12, bs=16 (AUC 0.9913 | EER 0.0833)짧은 윈도우의 승리: window_size가 12일 때 성능 극대화. SVD의 시간적 결함이 짧은 구간(Short-term)에 집중되어 노이즈 희석을 방지함.4. VideoMAE V1 (Temporal - SOTA)[고정 사양] Masking Ratio 0 (FT) / Grad Accumulation 4.이유: 12GB VRAM 한계를 극복하고 대규모 배치의 학습 효과를 재현함. (근거: Tong et al. 2022, Popel & Bo 2018)[그리드 탐색 구간 및 근거] Layer-wise LR Decay ($0.65 \sim 0.85$), LR ($5e^{-4} \sim 1e^{-4}$).이유: 거대 트랜스포머 모델의 사전 지식 보존 및 **Clark et al. (2020)**이 제안한 계층별 학습률 감쇄 기법의 유효성을 검증하기 위함임.[최적 설정 및 분석] lr=1e-4, layer_decay=0.65, bs=4, accum=4 (AUC 0.9618 | EER 0.0208)LR 민감도: $1e^{-4}$에서 안정적 급상승. 보수적 Layer Decay(0.65)로 하위 레이어의 사전 지식을 성공적으로 전이함.5. ConvNeXt + GRU (Hybrid - New)[고정 사양] ConvNeXt-Tiny (Frozen) / Orthogonal Init.이유: 백본은 동결하여 고품질 공간 특징을 유지하고, 직교 초기화를 통해 GRU의 학습 안정성을 확보함. (근거: Liu et al. 2022 / Cho et al. 2014, Saxe et al. 2013)[그리드 탐색 구간 및 근거] RNN Seq Length (16 vs 25), Dropout ($0.4 \sim 0.7$).이유: 긴 시퀀스 모델링 시 발생하는 RNN의 과적합 임계점을 정규화 강도를 통해 제어하기 위함임.[최적 설정 및 분석] lr=1e-3, seq_len=25, dropout=0.7, bs=16 (AUC 0.8932 | EER 0.1250)정보량과 규제: 25프레임 사용 시 정보량이 극대화되나, 과적합 제어를 위해 강력한 Dropout(0.7)이 필수적이었음.9. 평가 및 분석 방법론 (Methodology)통계적 유의성: 5-Fold 결과를 바탕으로 윌콕슨 부호 순위 검정 수행 (Demšar 2006).포렌식 근거: Grad-CAM 어텐션 시각화 및 FFT 주파수 에너지 분석을 통해 도메인 하락폭($\Delta$)의 물리적 원인을 규명함.