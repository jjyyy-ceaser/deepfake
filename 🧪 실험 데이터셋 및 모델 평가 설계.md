🧪 실험 데이터셋 및 모델 평가 설계 (최종 확정본 - Rev.18)1. 데이터 소스 (Source Data)가짜 영상 (Fake): Stable Video Diffusion (SVD)생성 방식: Stability AI의 최신 비디오 확산 모델인 SVD (img2vid-xt) 프레임워크 기반. 본 연구에서는 1_generate_svd_final.py를 통해 전처리된 실제 영상의 첫 번째 프레임을 조건부 입력(Conditional Input)으로 사용하여 생성함.특징: 잠재 공간 확산(Latent Diffusion)을 통해 참조 이미지의 컨텐츠를 유지하면서 시간적 일관성을 가진 비디오를 생성. 기존 GAN 방식보다 정교한 시공간적 특징을 가지나, 디퓨전 특유의 미세 노이즈 패턴이 존재함.근거: **Blattmann et al. (2023)**의 연구에 따르면, SVD는 고해상도 비디오 생성에서 SOTA 성능을 보이며, 이는 현재 가장 탐지하기 어려운 최신 생성 모델 도메인을 대표함.SVD 생성 시나리오 구체화: 변인 통제를 위해 SVD 생성 파라미터를 motion_bucket_id=127, noise_aug_strength=0.02, fps=25, seed=42로 고정함.이유: 생성 옵션의 차이가 아닌 조작 흔적 자체를 학습하기 위한 필수 설정임.근거: 해당 수치는 Stability AI에서 권장하는 표준값으로, 육안상 실제 영상과 구분이 어려운 중상급 품질(High-fidelity)의 동적 비디오를 생성하므로 탐지 모델의 변별력을 테스트하기에 가장 적합한 임계점임.실제 영상 (Real): CelebV-HQ (Original Sequences)수집 방식: 고화질 인물 비디오 데이터셋인 CelebV-HQ에서 300개의 원본 클립을 선별.특징: 실제 인물의 고해상도 안면 특징, 다양한 조명 조건, 자연스러운 미세 표정 변화를 포함함.근거: **Zhu et al. (2022)**에서 제안된 CelebV-HQ는 딥페이크 탐지 연구에서 실제 인간의 고품질 데이터 기준점(Baseline)으로 널리 사용됨.2. 플랫폼별 변조 매트릭스 (Distortion Matrix)실제 SNS 환경의 재인코딩 과정을 정밀 시뮬레이션하여 총 5개 도메인을 구축했습니다.도메인 (Domain)해상도 (Resolution)코덱압축 강도 (CRF)박스 시퀀스특징RawOriginalLossless0 (None)-학습 기준점 (Ground Truth)YouTube640x360H.26444.9ftyp-moov-mdat블록 노이즈 및 고압축 아티팩트Instagram1276x720H.26434.8ftyp-moov-free-mdat모바일 최적화 및 리사이징(Blur)Kakao_High1920x1080HEVC (H.265)22.4ftyp-free-mdat-moov고효율 코덱 변환 (Moov 후방 배치)Kakao_Normal1280x720H.26423.6ftyp-moov-free-mdat해상도 강제 축소 및 정보 손실2-1. 왜곡 분석 방법론 및 근거 (Analysis & Simulation Basis)해상도 및 코덱 분석: ffprobe를 이용해 SNS 다운로드 영상 스트림의 coded_width, codec_name 태그를 직접 파싱하여 플랫폼별 표준 값을 확정함.박스 시퀀스 분석: Python struct 모듈을 사용하여 MP4 컨테이너의 바이너리 헤더를 파싱. 근거: Yang et al. (2024) 연구에 기반하여 스트리밍 플랫폼(YouTube, Insta)이 빠른 재생(Fast Start)을 위해 메타데이터(moov)를 전방 배치함을 검증함.압축 강도 추정: 이유: 비트레이트 손실률을 통해 CRF 값을 역추산함. 근거: **Montibeller et al.**의 연구.3. 데이터셋 구성 및 분할 (Dataset Split)언페어드 분할 (Unpaired Balanced Split): CelebV-HQ(Real) 300개와 SVD(Fake) 300개를 결합하여 총 600개 클립으로 구성. 1:1 클래스 균형을 유지함.데이터 분할: 전체의 **80%(학습), 20%(테스트)**로 분리하여 인물의 외모가 아닌 조작 특징을 학습하도록 설계함. 특히, 데이터 분할 시 클립 단위를 넘어 **인물 식별자(Identity) 기반 분리(Identity-level Split)**를 수행함.이유: 데이터 누수(Data Leakage)를 원천 차단하고 모델의 일반화 능력을 객관적으로 평가함.교차 검증: 5-Fold Cross-Validation을 통해 데이터셋의 통계적 안정성을 확보함.4. 탐지 모델 라인업 (Model Lineup)세대별(Classic vs. SOTA) 및 구조별(Spatial vs. Temporal vs. Hybrid) 대표 모델 5종을 선정했습니다.A. Spatial (공간적): ① Xception (Classic), ② Swin Transformer-Tiny (New)B. Temporal (시간적): ③ R3D-18 (Classic), ④ VideoMAE V1 (New)C. Hybrid (하이브리드): ⑤ ConvNeXt-Tiny + GRU (New Hybrid)5. 학습 및 평가 전략 (Cross-Domain Evaluation)학습: Raw(원본) 데이터로만 학습하여 생성 모델의 순수 특징(Fingerprint) 추출에 집중함.평가 (Robustness Test): 학습된 모델을 5개 도메인에 적용하여 성능 하락폭(Delta)을 측정함.성능 지표: 탐지 모델의 포괄적 성능 평가를 위해 ①ACC, ②AUC, ③F1-Score, ④Precision, ⑤Recall 및 포렌식 신뢰성 검증을 위한 **⑥EER (Equal Error Rate)**과 **BPCER/APCER (Presentation Classification Error Rate)**를 산출하여 총 6가지 이상의 다각적 지표를 분석함.근거: ISO/IEC 19795-1 표준에 따라 위조를 진짜로 판정한 오류(APCER)와 진짜를 위조로 판정한 오류(BPCER)를 분리 보고함으로써 포렌식 신뢰도를 입증함.6. 전처리 및 입력 규격 (Data Preprocessing)입력 규격: Spatial/Hybrid/VideoMAE ($224 \times 224$), R3D ($112 \times 112$).표준화 파이프라인 (0_standardize.py):프레임 레이트 및 길이 동기화 (Temporal Synchronization): 모든 Real 및 Fake 영상을 **25 FPS / 25 프레임(1.0초)**으로 통일함.이유: 데이터셋 간의 미세한 시간적 해상도 차이가 모델의 학습 편향(Bias)으로 작용하는 것을 방지하고, Real의 높은 FPS와 Fake의 낮은 FPS 차이를 모델이 조작 흔적으로 오인하는 'Temporal Shortcut' 문제를 원천 차단하기 위함임. 특히 23.98 FPS와 같이 비표준 프레임 레이트를 가진 원본 데이터를 리샘플링하여 시간적 정보의 밀도를 균일하게 유지함.동적 비율 크롭 (Dynamic Scaling Crop): MTCNN을 통해 첫 프레임의 안면 위치를 탐지한 후, 얼굴 높이 대비 2.5배(scale=2.5) 영역을 16:9 비율로 계산하여 크롭함.이유: 머리카락 상단부터 턱 아래 목선까지의 핵심 정보를 안정적으로 포함하여, 딥페이크 탐지의 주요 지표인 안면 주변부 왜곡(Peripheral Artifacts)을 보존하기 위함임. (논문 기술 시에는 **"랜드마크 중심의 시공간적 변동성을 최소화한 Smart Fit 정렬"**이라고 표기함)위치 이동 로직 (Shift-based Boundary Handling): 크롭 영역이 영상 경계를 벗어날 경우 검은색 패딩 대신 **크롭 윈도우를 영상 안쪽으로 이동(Shift)**시킴.이유: SVD 모델이 검은색 여백을 비정상적 객체로 인식하여 생성 품질이 저하되거나 불필요한 노이즈가 개입되는 부작용을 방지하기 위함임.보간법: 리사이즈 시 고주파 성분 보존을 위해 Lanczos4 Interpolation을 적용함.이유: 주파수 도메인 분석 시 모델이 학습한 미세 텍스처 정보 손실을 최소화하기 위함임.안면 정렬 (Face Alignment): 매 프레임 MTCNN 또는 Dlib을 통해 안면 특징점(Landmark)을 추출하여 눈과 입의 위치를 정규화한 뒤 크롭을 진행함.근거: **Rossler et al. (2019)**에 따라, 안면 위치 고정 전처리를 통해 모델이 움직임 노이즈가 아닌 '안면 내부의 조작 불일치'에만 집중하도록 설계함.시간적 특징 추출 (Temporal Sampling): SVD의 25프레임 생성 결과물 중 촘촘한 샘플링(Dense Sampling)을 위해 Sliding Window 기반 Overlapping 샘플링을 적용함. (예: 1~16, 5~20, 9~25 프레임 구간 활용)근거: **Feichtenhofer et al. (2019)**의 연구에 근거하여, SVD 특유의 '프레임 모핑(Morphing)' 아티팩트를 놓치지 않기 위해 시간적 탐색 밀도를 극대화함.7. 하드웨어 최적화 및 증강 전략 (Hardware & Augmentation)장비 사양: NVIDIA RTX 4070 Super (VRAM 12GB) + AMD Ryzen 5 7500F환경 설정 (Environment Setup): OS: Ubuntu 22.04 LTS / Library: PyTorch 2.1+, CUDA 12.1, FFmpeg 6.0 / Pre-trained weights: torchvision v0.16 (ImageNet-1K v1), transformers v4.3x (Kinetics-400)이유: 사용 라이브러리 버전에 따른 연산 정밀도와 속도의 재현성을 보장하기 위함임.데이터 증강 전략 (Data Augmentation): 소규모 데이터셋($N=600$)의 한계를 극복하기 위해 Mixup ($\alpha=0.2$) 및 CutMix를 적용함.근거: **Zhang et al. (2017)**과 **Yun et al. (2019)**의 연구에 따라, 클래스 간 경계를 완화하고 특정 인물의 픽셀 암기를 방지하여 일반화 성능을 비약적으로 향상시킴.최적화: Mixed Precision (FP16) 및 VideoMAE용 Gradient Accumulation (4 Steps) 필수 적용.8. 연구 분석 방향성 (Analysis Focus)플랫폼 왜곡 요인 분석: 유튜브 압축(Spatial distortion) vs 카카오톡 해상도 저하(Temporal info density reduction)의 영향력 규명.이유: 실제 SNS 및 메신저 환경의 유통 상황에서 시간적 모델과 공간적 모델이 각각 어떤 강건성을 보이는지 입증하고, 성능 하락의 근본적인 원인을 도메인별 특징 파괴 관점에서 분석하는 데 목적이 있음.모델 강건성 비교: 플랫폼 왜곡 상황에서 급격히 상승하는 APCER을 추적하여 생성 흔적 파괴 원인 분석.정성적 분석 도구 (Qualitative Analysis):Grad-CAM: Raw 데이터에서 모델이 주목하던 안면 부위가 왜곡 이후 어떻게 변하는지 비교 분석함. (근거: Selvaraju et al., 2017)FFT: SVD 생성물의 고주파 성분이 플랫폼 압축을 거치며 어떻게 소멸되는지 주파수 도메인에서 제시함.9. 모델별 통합 정밀 사양 및 하이퍼파라미터 최적화 계획 (Architectural Specification)9-1. 실험의 형평성을 위한 데이터 샘플 수 통일 전략 (Data Balancing Strategy)본 연구는 모델 구조(Spatial vs. Temporal vs. Hybrid)에 관계없이 모든 모델이 동일한 정보량을 학습하도록 다음과 같이 샘플링 수를 통일합니다.물리적 데이터(Clip) 통일: * 이유: 입력 소스의 동일성을 보장하여 특정 데이터셋에 의한 성능 편향을 제거함.논리적 데이터(Sample) 수 통일 (Normalization of Training Samples):전략: Temporal/Hybrid 모델이 중첩 샘플링을 통해 클립당 3개의 시퀀스(총 1,800개)를 생성할 경우, Spatial 모델 역시 클립당 3장의 프레임을 독립적으로 추출하여 총 1,800개로 일치시킴.이유: 600개의 물리적 클립은 대규모 파라미터를 가진 SOTA 모델을 학습시키기에 통계적 유의성이 부족함. 중첩 샘플링을 통해 유효 데이터 수를 증폭함으로써 모델이 특정 인물의 외모를 단순 암기하는 것을 방지하고, 소규모 데이터셋 환경에서도 수렴 안정성을 보장하기 위함임. (구현 시 dataset.py의 samples_per_clip = 3 설정을 통해 Total logical samples: 1800을 보장함)근거: **Rezatofighi et al. (2019)**의 연구에 따르면, 학습 샘플당 역전파(Backpropagation) 횟수를 동일하게 맞추는 것이 구조적 우위를 증명하는 가장 객관적인 방법임.학습 반복 횟수(Iteration)의 균형: * 이유: 모델이 데이터에 노출되는 빈도를 통제함으로써 공정한 성능 비교 지표를 확보함.근거: **Smith et al. (2018)**의 "Don't Decay the Learning Rate, Increase the Batch Size" 논리.9-2. 모델별 세부 사양 및 가중치 운용 전략모델 라인업추천 가중치 (Pre-trained)획득처 및 특징교체 난이도XceptionFaceForensics++ (FF++)GitHub 오픈소스. 딥페이크 탐지의 '바이블' 가중치입니다.낮음 (강추)Swin-TImageNet-21K일반 IN-1K보다 데이터가 21배 많아 특징 추출 능력이 더 정교합니다.보통R3D-18Kinetics-400비디오 행동 인식 표준 가중치. 움직임을 알기 때문에 그대로 써도 무방합니다.낮음 (유지 가능)VideoMAEKinetics-400라이브러리 호환성 및 안정적인 학습을 위해 Kinetics-400 가중치를 사용함.낮음 (유지 가능)ConvNeXtImageNet-1K (Frozen)백본은 고정하고 뒤의 GRU만 학습시키는 전략을 취합니다.매우 낮음Case A: 딥페이크 가중치로 교체했을 때 (강력 추천)"본 연구는 소규모 데이터셋($N=600$)에서의 도메인 적응력을 높이기 위해, 공간 탐지 모델인 Xception에 FaceForensics++ 데이터셋으로 사전 학습된 가중치를 전이 학습(Transfer Learning)하였다. 이를 통해 일반 사물 인식이 아닌 안면 변조 흔적(Artifact) 추출에 특화된 특징 맵을 초기값으로 확보하였다."1. Xception (Spatial - Baseline)[고정 사양]아키텍처: $224 \times 224 \times 3$ / MTCNN Face Alignment / Frozen (First 2 Blocks) (근거: Rossler et al. 2019, Yosinski et al. 2014)이유: 하위 레이어는 일반적인 엣지 특징을 추출하므로 동결(Frozen)하여 사전 학습된 지식을 보존하고, 상위 레이어에서 SVD 특유의 질감 아티팩트를 학습하도록 유도함.[그리드 탐색 구간 및 근거]Learning Rate (LR): $1e^{-3} \sim 1e^{-5}$ / Dropout Rate: $0.2 \sim 0.6$ (근거: Srivastava et al., 2014)[그리드 서치 결과 및 최적값 선별 근거]최적값: lr=1e-3, dropout=0.4, bs=32성능: AUC 0.9987 | EER 0.0000 | APCER 0.0000분석: * High LR 선호: 다른 모델들이 1e-4에서 최적이었던 것과 달리, Xception은 1e-3이라는 높은 학습률에서 최고 성능을 냈습니다. 이는 전이 학습(Transfer Learning) 시 FF++ 가중치가 이미 수렴점에 가깝기 때문에, 과감한 업데이트가 오히려 지역 최적점(Local Minima) 탈출에 유리했음을 보여줍니다.Dropout의 중용: 0.2(과적합 위험)나 0.6(학습 저해)보다 0.4에서 AUC가 가장 높았습니다. 균형 잡힌 정규화가 핵심이었습니다.2. Swin Transformer-Tiny (Spatial - SOTA)[고정 사양]윈도우/패치: $7 \times 7$ Window / $4 \times 4$ Patch (근거: Liu et al. 2021)이유: 사전 학습된 Relative Position Bias와의 호환성을 위해 고정이 필수적이며, $4 \times 4$ 패치는 디퓨전 모델의 미세한 텍스처 불일치를 포착하는 최소 단위임.정규화: Stochastic Depth 0.2 (근거: Huang et al. 2016)데이터 증강: CutMix (근거: Yun et al. 2019)[그리드 탐색 구간 및 근거]Learning Rate (LR): $1e^{-4} \sim 1e^{-5}$ / Weight Decay: $0.01 \sim 0.1$ (근거: Touvron et al., 2021)[그리드 서치 결과 및 최적값 선별 근거]최적값: lr=1e-4, weight_decay=0.1, bs=16성능: AUC 1.0000 | EER 0.0000 | APCER 0.0000분석: * 완벽한 분리: 1-Fold 탐색에서 이미 완벽한 변별력을 보였습니다. 이는 Swin의 **계층적 윈도우 어텐션(Hierarchical Window Attention)**이 딥페이크의 미세한 픽셀 부조화를 포착하는 데 매우 강력함을 시사합니다.규제의 승리: Weight Decay가 0.1로 가장 높을 때도 완벽한 성능을 유지했습니다. 이는 과적합을 강하게 억제해도 특징이 소실되지 않을 만큼 모델의 표현력이 풍부하다는 증거입니다.3. R3D-18 (Temporal - Baseline)[고정 사양]해상도/커널: $112 \times 112$ / $(3 \times 3 \times 3)$ Kernel (근거: Tran et al. 2018)이유: 3D 연산량 폭증을 제어하여 RTX 4070S 환경에서 배치 16 이상을 유지하기 위한 연산 효율화 전략임.샘플링 전략: Dense Sampling (Stride 1) (근거: Feichtenhofer et al. 2019)[그리드 탐색 구간 및 근거]Learning Rate (LR): $1e^{-3} \sim 1e^{-4}$ / Temporal Window Size: $12 \text{ vs } 16 \text{ Frames}$ (근거: SVD 생성 영상의 시간적 불안정성 포착 밀도 검증)[그리드 서치 결과 및 최적값 선별 근거]최적값: lr=1e-4, window_size=12, bs=16성능: AUC 0.9913 | EER 0.0833 | APCER 0.1250분석: * 짧은 윈도우의 승리: window_size가 16일 때보다 12일 때 AUC가 더 높았습니다(0.99 vs 0.97). SVD 생성 영상의 시간적 결함(떨림, 모핑 등)이 짧은 구간(Short-term) 내에 집중되어 있어, 윈도우가 길어지면 오히려 노이즈가 희석되는 것으로 판단됩니다.4. VideoMAE V1 (Temporal - SOTA)[고정 사양]학습 설정: Masking Ratio 0 (FT) / Grad Accumulation 4 Steps (근거: Tong et al. 2022, Popel & Bo 2018)이유: 12GB VRAM의 물리적 한계를 극복하기 위해 그래디언트 누적을 사용하여 대규모 모델의 안정적인 학습 효과를 재현함. 라이브러리 호환성 및 안정적인 학습을 위해 VideoMAE V1(Kinetics-400 pre-trained)을 사용함.[그리드 탐색 구간 및 근거]Layer-wise LR Decay: $0.65 \sim 0.85$ (근거: Clark et al. 2020) / Learning Rate (LR): $5e^{-4} \sim 1e^{-4}$[그리드 서치 결과 및 최적값 선별 근거]최적값: lr=1e-4, layer_decay=0.65, bs=4, accum=4성능: AUC 0.9618 | EER 0.0208 | APCER 0.1042분석: * LR 민감도: 5e-4에서는 AUC가 0.5대로 추락하며 학습에 실패했으나, 1e-4로 낮추자마자 0.96대로 급상승했습니다. 거대 모델인 만큼 학습률에 매우 민감합니다.보수적 튜닝: layer_decay가 0.65로 가장 낮을 때(하위 레이어를 거의 보존할 때) 성능이 가장 좋았습니다. Kinetics-400에서 학습된 동작 인식 지식이 딥페이크 탐지에도 유효함을 입증합니다.5. ConvNeXt + GRU (Hybrid - New)[고정 사양]백본 구조: ConvNeXt-Tiny (Frozen) / Orthogonal Init (근거: Liu et al. 2022, Saxe et al. 2013)이유: 특징 추출기인 백본은 동결하여 고품질 공간 특징을 유지하고, 직교 초기화(Orthogonal)를 통해 GRU의 장기 의존성 학습 안정성을 확보함.시계열 유닛: GRU (Hidden 512). GRU의 최종 은닉 상태(Final Hidden State)뿐만 아니라 모든 시점의 은닉 상태를 시간축에 대해 평균 풀링(Global Average Pooling over Time)하여 시퀀스 전체의 시공간 특징 정보를 손실 없이 보존함. (근거: Cho et al. 2014)[그리드 탐색 구간 및 근거]RNN Sequence Length: $16 \text{ vs } 25 \text{ (Full)}$ / Classifier Dropout: $0.4 \sim 0.7$ / Learning Rate (LR): $1e^{-3} \sim 1e^{-4}$[그리드 서치 결과 및 최적값 선별 근거]최적값: lr=1e-3, seq_len=25, dropout=0.7, bs=16성능: AUC 0.8932 | EER 0.1250 | APCER 0.1875분석: * 정보량과 규제: 시퀀스 길이가 **25(Full Frame)**일 때 성능이 가장 좋았으나, 동시에 Dropout 0.7이라는 매우 강한 규제가 필요했습니다. 이는 RNN 계열이 긴 시퀀스에서 과적합되기 쉬운 특성을 그대로 보여주며, 이를 강한 드롭아웃으로 제어했을 때 최적 성능이 나옴을 확인했습니다.9-3. 공통 최적화 및 과적합 방어 기법손실 함수: Label Smoothing (0.1) 적용.이유: 소규모 데이터셋에서 발생하는 'Overconfidence' 문제를 방지하고 플랫폼 왜곡에 대한 강건성을 확보함.근거: Szegedy et al. (2016).추가 문구: "본 실험은 모델 간 공정한 비교를 위해 물리적 클립 수(600개)를 넘어, 실제 학습에 투입되는 유효 샘플 수(Effective Training Samples)를 모든 모델에 대해 1,800개로 통일하였다. 시각적 정보량의 평형을 맞춤으로써, 도메인 하락폭(Delta)의 차이가 데이터 수의 차이가 아닌 모델 구조(Spatial vs. Temporal)의 본질적인 강건성에서 기인함을 입증한다."10. 평가 및 분석 방법론 (Evaluation & Analysis Methodology)10.1 통계적 유의성 분석: 5-Fold 결과를 바탕으로 **윌콕슨 부호 순위 검정(Wilcoxon Signed-rank Test)**을 수행함.이유: 단순한 ACC 평균 비교가 아닌 비모수 검정을 통해 구조적 우위의 통계적 유효성을 증명함.근거: Demšar (2006).10.2 포렌식 근거 역추적 (Explainability): 플랫폼 왜곡이 탐지 성능에 미치는 물리적 영향을 분석하기 위해 **Grad-CAM(Selvaraju et al., 2017)**을 적용한다.시각적 증거(Explainability): Grad-CAM을 사용하여 공간 모델은 '안면의 텍스처'에, 시간 모델은 '입 모양의 움직임'에 어텐션이 집중됨을 보여주고, 왜곡 발생 시 이 어텐션 맵이 어떻게 흩어지는지 시각적으로 증명한다.이유: 모델의 판단 근거를 시각화하여 포렌식 도구로서의 타당성을 입증함.주파수 도메인 분석: 픽셀 데이터(공간)를 FFT(고속 푸리에 변환)하여 **주파수 대역별 평균 에너지 강도 변화율(Log-frequency Power Spectrum)**을 분석한다.이유: SNS 압축이 '공간 정보'를 먼저 파괴한다는 가설을 물리적 데이터로 뒷받침하고 정보 손실 임계점을 정밀 규명함.10.3 데이터 증강 효과 및 과적합 방어: * 이유: 부족한 데이터 환경에서 제안된 전처리 전략이 모델 성능 유지에 미친 실질적 영향력을 규명함.* 근거: Zhang et al. (2017).